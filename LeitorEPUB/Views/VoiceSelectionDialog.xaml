<Window x:Class="LeitorEPUB.Views.VoiceSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Selecionar Voz"
        Height="500" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="300" MinWidth="350"
        Background="{DynamicResource BackgroundBrush}"
        WindowStyle="ToolWindow">
    <!-- WindowStyle="ToolWindow" evita barra de título branca -->
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="DialogTitle" Grid.Row="0"
                   Text="Escolha uma voz para leitura:"
                   FontSize="14" FontWeight="Bold"
                   Foreground="{DynamicResource ForegroundBrush}"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Filtrar:" 
                       Foreground="{DynamicResource ForegroundBrush}" 
                       Margin="0,0,0,4"/>
            <TextBox x:Name="FilterTextBox"
                     Padding="6"
                     Background="{DynamicResource TextBackgroundBrush}"
                     Foreground="{DynamicResource ForegroundBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     TextChanged="FilterTextBox_TextChanged"/>
        </StackPanel>

        <Border Grid.Row="2" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ListBox x:Name="VoiceListBox"
                         Background="{DynamicResource TextBackgroundBrush}"
                         Foreground="{DynamicResource ForegroundBrush}"
                         BorderThickness="0"
                         MouseDoubleClick="VoiceListBox_MouseDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2">
                                <TextBlock Text="{Binding Description}" 
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource ForegroundBrush}"/>
                                <TextBlock Text="{Binding Name}" 
                                           FontSize="11" 
                                           Opacity="0.7"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="Margin" Value="0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="OkButton" Content="OK" Width="80" Height="30"
                    Click="OkButton_Click" IsDefault="True"
                    Margin="0,0,10,0"
                    Background="{DynamicResource ChoiceBrush}"
                    Foreground="{DynamicResource ForegroundBrush}"/>
            <Button x:Name="CancelButton" Content="Cancelar" Width="80" Height="30"
                    Click="CancelButton_Click" IsCancel="True"
                    Background="{DynamicResource ChoiceBrush}"
                    Foreground="{DynamicResource ForegroundBrush}"/>
        </StackPanel>
    </Grid>
</Window>